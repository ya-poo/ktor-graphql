type Book {
    id: ID!
    name: String!
    author: Author!
    reviews: [Review!]!
}

type Review {
    id: ID!
    stars: Int!
    commentary: String!
}

type Author {
    id: ID!
    name: String!
    country: Country!
}

enum Country {
    Japan
}
